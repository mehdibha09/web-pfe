# TODO

- [ ] Interpret the user request (“test”) as: build a dashboard Principal page (in French) showing global system status.
- [ ] Inspect existing layout/routes/components to integrate the new dashboard view.
- [ ] Implement the new view/page under `src/views/dashboard`.
- [ ] Add/route link(s) in sidebar/navigation.
- [ ] Implement mocked data/service calls for services, deployments, alerts, CPU/RAM.
- [ ] Run `npm run build` and `npm run lint` to verify.
- [ ] Provide final summary and how to access the new dashboard.
