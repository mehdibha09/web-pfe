server:
    port: 6060

spring:
    application:
        name: gateway

    cloud:
        gateway:
            default-filters:
                - CorrelationId
            routes:
                - id: auth-login
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/auth/login
                  filters:
                      - name: RateLimit
                        args:
                            maxRequests: 20
                            windowMs: 60000

                - id: auth-forgot-password
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/auth/forgot-password,/api/v1/auth/reset-password
                  filters:
                      - name: RateLimit
                        args:
                            maxRequests: 5
                            windowMs: 60000

                - id: auth-public
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/auth/refresh,/api/v1/auth/sso/**

                - id: auth-service
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/auth/**
                  filters:
                      - Auth

                - id: auth-users
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/users/**
                  filters:
                      - Auth

                - id: auth-roles
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/roles/**
                  filters:
                      - Auth

                - id: auth-permissions
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/permissions/**
                  filters:
                      - Auth

                - id: auth-tenants
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/tenants/**
                  filters:
                      - Auth

                - id: auth-sessions
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/sessions/**
                  filters:
                      - Auth

                - id: auth-audit
                  uri: ${AUTH_SERVICE_URL:http://localhost:7070}
                  predicates:
                      - Path=/api/v1/audit-logs/**
                  filters:
                      - Auth
                      - name: RateLimit
                        args:
                            maxRequests: 30
                            windowMs: 60000

                - id: services
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/services/**
                  filters:
                      - Auth

                - id: environments
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/environments/**
                  filters:
                      - Auth

                - id: service-environments
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/service-environments/**
                  filters:
                      - Auth

                - id: deployments
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/deployments/**
                  filters:
                      - Auth

                - id: metrics
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/metrics/**
                  filters:
                      - Auth
                      - name: RateLimit
                        args:
                            maxRequests: 60
                            windowMs: 60000

                - id: vm
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/vms/**
                  filters:
                      - Auth

                - id: backups
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/backups/**
                  filters:
                      - Auth

                - id: ssh
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/ws/ssh/**
                  filters:
                      - Auth

                - id: k8s
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/k8s/**
                  filters:
                      - Auth

                - id: deployment-templates
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/deployment-templates/**
                  filters:
                      - Auth

                - id: costs
                  uri: ${CLOUD_PRICER_SERVICE_URL:http://localhost:8090}
                  predicates:
                      - Path=/api/v1/costs/**
                  filters:
                      - Auth

                - id: quotas
                  uri: ${CLOUD_PRICER_SERVICE_URL:http://localhost:8090}
                  predicates:
                      - Path=/api/v1/quotas/**
                  filters:
                      - Auth

                - id: pricing
                  uri: ${CLOUD_PRICER_SERVICE_URL:http://localhost:8090}
                  predicates:
                      - Path=/api/v1/pricing/**
                  filters:
                      - Auth

                - id: alerts-pricer
                  uri: ${CLOUD_PRICER_SERVICE_URL:http://localhost:8090}
                  predicates:
                      - Path=/api/v1/alerts/**
                  filters:
                      - Auth

                - id: notifications
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/notifications/**
                  filters:
                      - Auth

                - id: test
                  uri: ${DEPLOYMENT_SERVICE_URL:http://localhost:8082}
                  predicates:
                      - Path=/api/v1/test/**

            httpclient:
                response-timeout: 300s
                connect-timeout: 5000

            globalcors:
                corsConfigurations:
                    '[/**]':
                        allowedOriginPatterns:
                            - '*'
                        allowedMethods:
                            - GET
                            - POST
                            - PUT
                            - PATCH
                            - DELETE
                            - OPTIONS
                        allowedHeaders: '*'
                        exposedHeaders:
                            - Authorization
                            - Content-Disposition
                        allowCredentials: true
